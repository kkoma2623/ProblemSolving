#include <iostream>
#include <vector>

using namespace std;

int main(){
  int testCases;
  cin >> testCases;

  for(int cases=0; cases<testCases; cases++){
    int M,N,K;
    cin >> M >> N >> K;
    vector<int> temp(M);
    // vector<vector<int> > ground(temp, 0);
  }

  return 0;
}